# 变更日志

## workspace.1.1 — 2026-08-03

基于 1.0 的逐模块优化，UI 整体换肤为米黄+灰色简约风，新增全局 AI 助手，知识库转存可配置化。

### 新增
- **全局 AI 助手**：右下角浮动按钮，上拉对话抽屉，支持收起 / 一键清空聊天记录；实时汇总工作台数据（今日收支、待办、英语打卡、运动、AI 资讯）作为依据，可回答「今天花了多少钱」「新技术日报」等；未配置模型时引导去设置。
- **设置 · 模型可选 + 连通性测试**：模型设置支持选择具体模型（DeepSeek V3/R1、硅基流动 7 款、零一万物 4 款 + 自定义），「测试连接」真实调用验证 Key 与模型是否生效，并显示「当前生效模型」；外观设置新增浅色/深色/跟随系统切换（全局联动）。
- **运动 · 每类别教程链接**：新增 `sport_links` 表，训练资料页与今日计划卡可按类别管理教程链接，自动识别 📕小红书 / 🎵抖音 / 📺B站 / ▶️YouTube / 🔗其他，点击在新标签打开，手机端可唤起对应 App。
- **AI 资讯 · 转存知识库**：单条/批量「转存飞书」升级为「转存知识库」菜单，可选飞书多维表格 / 腾讯文档 / 复制 Markdown；未配置的知识库自动置灰并提示去设置。
- **下载 taste-skill 并改造 UI**：安装 `minimalist-ui` + `redesign-existing-projects` 两套指令，整体配色改为暖米黄 `#F6F3EC` 底 + 暖黏土 `#A06A43` 主色 + 暖灰侧栏，卡片加暖描边、毛玻璃顶栏、焦点环、选中高亮等细节。

### 变更
- **英语学习优化**：短语/句子/段落/对话各块加「🔄换一批」刷新；每块加 OK 按钮 + 进度条，刷新重置进度；打卡记录页精简为「连续天数 + 月历」，删录入内容与最近记录；学习单元加「＋添加单元」入口（AI 生成或复制提示词导入 JSON）。
- **财务管理优化**：记账按钮底部居中悬浮；顶部两张并列收支卡（今日收入/支出）；明细区搜索 + 右侧「⏱筛选」按时间周期/分类快速筛选；已创建记录支持编辑（复用记账弹窗预填）；编辑/删除改为右侧图标按钮（✏️ / 🗑）。
- **设置重组**：按「模型设置 / 知识库设置 / 资料资源设置 / 数据管理 / 外观」分区，数据管理导入改为动态加载各 DB。
- IndexedDB `DB_VERSION` 由 3 升至 4，新增 `sport_links` 表（自动升级，历史数据不受影响）。

### 移除
- **历史归档模块**：删除路由、侧边栏入口与 `HistoryArchive.vue` 文件（历史数据仍保留在 IndexedDB，仅不再单独展示）。

### 修复
- 记账记录此前无法编辑。
- 视图中残留的硬编码 AI 紫（`Finance.vue` 折线图、`English.vue` 提示卡）统一改为主题色。

## workspace.1.0 — 2026-07-31

首个稳定版本，八大模块全部可用，纯前端离线运行，已部署至 GitHub Pages。

### 新增
- **英语学习**（独立模块 `/english`）：内置 10 个主题单元（寒暄、职场、会议、产品、观点、日常、健身、旅行、情绪、面试），每单元含短语、句子、段落、对话与口语提示；Web Speech 朗读、语速调节、中英切换、影子跟读、逐条收藏、整单元打卡、连续打卡统计与月度日历。
- **运动计划**（独立模块 `/sport`）：周循环计划编辑、快速打卡、本周统计、7 天趋势柱状图；5 套训练指南（游泳、肩背、臀腿、爬坡、攀岩）含热身/动作/放松/常见错误；视频库支持 B站/YouTube 链接内嵌播放与本地视频上传（≤200MB，存 IndexedDB Blob，完全离线）。
- **AI 资讯 RSS 采集**：新增 `services/news.ts`，内置 11 个免费公开 RSS 源，经 4 个公共 CORS 代理依次 fallback 抓取；DOMParser 解析 RSS 2.0 / Atom；关键词自动分类为新技术/新产品/新开源/新点子；按 link 去重、30 天自动清理未收藏项。
- **资讯源管理**：设置页可开关内置源、添加自定义 RSS 地址、配置腾讯文档入口。
- **柴火燃烧音效**：专注模式用 Web Audio 实时合成低频轰鸣底噪 + 随机噼啪爆裂，音量滑块实时生效。
- **记账图片双入口**：看板与财务页均提供「📷 拍照」「🖼 相册」两个独立按钮。

### 变更
- 「学习与健康」合并模块拆分为「英语学习」与「运动计划」两个独立模块。
- AI 资讯不再强依赖飞书，改为本地 IndexedDB 存储；飞书与腾讯文档降级为可选转存出口（批量转存飞书 / 复制 Markdown）。
- IndexedDB `DB_VERSION` 由 1 升至 2，新增 `news_items`、`sport_videos` 两张表（自动升级，历史数据不受影响）。
- 部署方式由 GitHub Actions 改为手动 `deploy.sh` 推送 gh-pages 分支。

### 移除
- 「探店副业」模块（页面、路由、导航、飞书 `shop_materials` 表配置、`services/llm.ts` 全部清除）。
- 专注模式的粉红噪音。
- 飞书 `ShopMaterialsAPI` / `DailyBriefsAPI` 与「免费数据源 API」设置项。

### 修复
- 记账图片此前只能拍照、无法从相册选择。
- 记账记录的图片无法单独删除（现支持只清图片、保留记录）。
